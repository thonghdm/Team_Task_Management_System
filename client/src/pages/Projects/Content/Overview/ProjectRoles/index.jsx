import React from 'react';
import { Box } from '@mui/material';

const ProjectRoles = ({ id, name, image, email }) => {
  const text = id === 1 ? "Owner" : "Member";

  return (
    <Box>
      <Box>
      </Box>
    </Box>
  );
};

export default ProjectRoles;